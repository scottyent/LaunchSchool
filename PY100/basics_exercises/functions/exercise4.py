# Write a function that accepts a single argument, a number, and returns the
# result of multiplying its argument by an exponent of 2 (also known as
# squaring the number or raising the number to the power of 2).

def square(num):
    return num**2

print(square(2))
print(square(3))