# In the following code snippet, find all violations of the PEP8 style guide.
# Rewrite it so that it conforms with the guide.

ice_cream_density = 10 # not following snake_case, fixed the spacing around =

while ice_cream_density > 0: # adding in white space around > but removing it before colon
    print('Drip...')
    ice_cream_density -= 1 # adding in white space around the augmented assignment

print('The ice cream melted.')